import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ef5350'
    },
    secondary: {
      main: '#FFC81A'
    }
  }
});

export default theme;
